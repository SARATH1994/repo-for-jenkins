pipeline {
    agent { docker 'maven:3-alpine' } 
    stages {
        stage('Example Build') {
            steps {
                sh 'mvn -B clean verify'
                sh 'mvn sonar:sonar -Dsonar.host.url=http://52.66.187.124:9000'
            }
        }
    }
}
