  
pipeline {
    agent { docker 'maven:3-alpine' } 
    stages {
        stage('Example Build') {
            steps {
                
                sh 'mvn sonar:sonar -Dsonar.host.url=http://13.235.115.209:9000'
                sh 'mvn clean install'
            }
        }
    }
}
